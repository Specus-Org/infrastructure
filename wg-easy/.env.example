# wg-easy admin UI password (bcrypt hash)
#
# Generate with:
#   docker run --rm ghcr.io/wg-easy/wg-easy wgpw 'YOUR_PASSWORD'
#
# Then paste the output here (the full $2b$... string).
WG_PASSWORD_HASH=$2b$12$REPLACE_WITH_BCRYPT_HASH
